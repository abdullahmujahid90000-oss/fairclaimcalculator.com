node_modules
dist
.astro
*.log

# Generated by scripts/generate-legacy-stubs.mjs (an npm `prebuild` step —
# see package.json). Regenerated fresh from scripts/legacy-paths.mjs on
# every build, locally and in CI, so committing them would only invite
# drift between the manifest and a stale checked-in copy. The manifest and
# generator script are the real source of truth and ARE committed.
/public/about.html
/public/contact.html
/public/disclaimer.html
/public/privacy-policy.html
/public/terms-of-service.html
/public/settlement-check-breakdown/
/public/total-loss-offer-calculator/
/public/diminished-value-calculator/
/public/claim-letter-builder/
/public/calculators/
/public/articles/
